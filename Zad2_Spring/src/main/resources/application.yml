spring:
  profiles:
    active: ${APP_PROFILE:jdbc, jpa}

server:
  port: ${PORT:8080}

jwt:
  secret: ${JWT_SECRET:5Sy1-R-Zj7ajgcabswkEttARiS8Q4Vs9IhYCVAt1bQ0}
  expiration: ${JWT_EXPIRATION:3600000}

---
spring:
  config:
    activate:
      on-profile: json
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

---
spring:
  config:
    activate:
      on-profile: jdbc, jpa
  datasource:
    url: jdbc:postgresql://ep-orange-pond-alu78tcy-pooler.c-3.eu-central-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_DKALX2G8Psaz
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect